# Implement

1. session `createdAt`/`updatedAt` + list 展示 + 测试
2. `waitUntilLive` in connectOrEnable + runtime 测试
3. cleanup 不误杀共享 runtime + 测试
4. attach starting 自愈 + 显式 auto-port attach / 冲突修正
5. build + cli-behavior/runtime/core
6. 真机 smoke 记录
7. 必要时 skill/session-contracts 一句

Rollback: revert 上述文件。
